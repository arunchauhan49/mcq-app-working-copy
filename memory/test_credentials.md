# Test Credentials

Users register through the app. For the testing agent, use a fresh registration:

- Email: `student+test@example.com`
- Password: `Test1234!`
- Name: `Test Student`

If the account already exists, use the login endpoint. Otherwise register via POST /api/auth/register.
